void setup() {
  size(500, 500);
}

void draw() {
  background(0);
  fill(255);
  int xs = mouseX^2;
  int ys = mouseY^2;
  int rs = height^2;
  text (height*acos(mouseX), 20, 20);
text (sqrt((-1*mouseX*mouseX)+250000), 20, 20);
  triangle(0, height, width, height, mouseX, sqrt((-1*xs)+rs));
}
