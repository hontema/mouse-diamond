void setup() {
  size(500, 500);
}

void draw() {
  background(0);
  fill(255);

  //text (height*acos(mouseX), 20, 20);
  text (sqrt((-1*mouseX*mouseX)+250000), 400, 400);
  triangle(0, 0, width, 0, mouseX, sqrt((-1*mouseX*mouseX)+250000));
}
